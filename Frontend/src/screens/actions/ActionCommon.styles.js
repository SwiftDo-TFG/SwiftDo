import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({

    action: {
        flexDirection: 'row',
        alignItems: 'center',
        width: '65%',
        marginVertical: 60,
        marginLeft: 20
    },
    iconAction: {
        fontSize: 26,
        marginRight: 10,
        width: '20%',
        textAlign: 'center'
    },
    actionTitle: {
        fontSize: 24,
        fontWeight: 'bold'
    },
    emptyIcon: {
        fontSize: 40
    }
})

export default styles;